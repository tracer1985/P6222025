---
- name: Test SSH connection to Windows host as Administrator
  hosts: all
  gather_facts: no
  tasks:
    - name: Get Windows hostname
      win_command: hostname
      register: hostname_result

    - name: Show hostname
      debug:
        var: hostname_result.stdout
